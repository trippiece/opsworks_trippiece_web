DATABASES = {
    'default': {
        'ENGINE': 'django.db.backends.mysql',
        'NAME': '<%= node[:db][:name] %>',
        'USER': '<%= node[:db][:user] %>',
        'PASSWORD': '<%= node[:db][:password] %>',
        'HOST': '<%= node[:db][:host] %>',
        'PORT': '',
    }
}