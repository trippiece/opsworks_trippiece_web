import urllib


# Make this unique, and don't share it with anybody.
SECRET_KEY = '<%= node[:app][:secretkey] %>'

# gcm
GCM_API_KEY = '<%= node[:app][:gcmapikey] %>'

# AWS
AWS_ACCESS_KEY_ID = '<%= node[:aws][:key] %>'
AWS_SECRET_ACCESS_KEY = '<%= node[:aws][:secret] %>'

# celery setup
BROKER_URL = 'sqs://%s:%s@' % (AWS_ACCESS_KEY_ID, urllib.quote(AWS_SECRET_ACCESS_KEY, safe=''))

# SNS
SNS_TOPIC_ARN = '<%= node[:sns][:topic_arn] %>'
SNS_SUBSCRIPTION_ARN = '<%= node[:sns][:subscription_arn] %>'

# Raven/Sentry
RAVEN_CONFIG = {
    'dsn': '<%= node[:raven][:dsn] %>',
}

<%- if node[:app][:bot_authtoken] %>
# Official Bot's authentication token.
BOT_AUTHTOKEN = '<%= node[:app][:bot_authtoken] %>'
<%- end %>

SEO_JS_PRERENDER_TOKEN = '<%= node[:app][:seo_js_prerender_token] %>'

FACEBOOK_APP_SECRET = '<%= node[:app][:facebook_secret] %>'